<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" x:Class="PL.MainWindow"   
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <Button Content="Button" HorizontalAlignment="Left" Margin="153,137,0,0" VerticalAlignment="Top" Width="75" Click="Button_Click" />
        <ListBox x:Name="listOfRes" HorizontalAlignment="Left" Height="400" Margin="313,10,0,0" VerticalAlignment="Top" Width="368">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <materialDesign:Card Padding="32" Margin="16" Background="HotPink">

                        <StackPanel>
                            <TextBlock Text= "{Binding name}"/>
                            <TextBlock Text= "{Binding manufacturer}"/>
                            <TextBlock Text= "{Binding cost}"><Run Text="₪"/></TextBlock>

                            <ContentPresenter>
                                <ContentPresenter.Content>
                                    <Image Source="{Binding imagePath}" Width="220" Height="220"/>
                                </ContentPresenter.Content>
                            </ContentPresenter>

                        </StackPanel>
                    </materialDesign:Card>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Label Content="{Binding}" HorizontalAlignment="Left" Margin="185,53,0,0" VerticalAlignment="Top" Height="36" Width="95"/>


       
    </Grid>


</Window>